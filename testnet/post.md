NASP-ACL-20260203-024.xls
NASP-ACL-20260225-017.xls
上述为两个网络申请表，其中E列为访问源的描述信息，F列为具体地址，每个表格内可填多个地址，H列为访问目标信息描述，I列为具体地址，L列为端口，也可为多个。
请针对上述表格样式，编写一个项目，该项目可以实现对源地址到目标地址的所有ip和端口的网络检测，检测结果需表述清楚，可以看到是哪个访问源的哪个地址到目标的哪个地址不通，如果访问源到目标的所有地址都联通，则仅需提示访问源到访问目标完全联通。
补充：该项目部署在一台单独的服务器上，该服务器与源地址之间的网络联通，与目标地址之间网络不通，需要完成上述需求。
仅作生产前的简单网络验证，在执行时一次网络验证，并不需要长期监控。
